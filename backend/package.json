{
  "name": "wpe-backend",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "migration:map": "node -e \"console.log(JSON.stringify(JSON.parse(require('fs').readFileSync('migration-map.json','utf8')),null,2))\""
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "http-proxy-middleware": "^3.0.5",
    "pg": "^8.20.0"
  }
}
